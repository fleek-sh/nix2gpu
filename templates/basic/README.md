# Basic `nix2gpu` template

This template includes the most basic possible template for creating a `nix2gpu` container.

It does not come with any of the nice functionality that comes with enabling some of the other flake inputs, such as `services` generation, `home-manager` integration or better containers from `nix2container`.
