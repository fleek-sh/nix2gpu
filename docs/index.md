# // `nix2vast` // documentation

Welcome to the official `nix2vast` documentation.

# // `nix2vast` // about

`nix2vast` is an easy and powerful nix-based GPU container runtime for deploying AI workloads to vast (and soon similar platforms) automatically and simply in the large scale.

## // `nix2vast` // get started

Get started by reading our [getting started guide](./getting-started.md).

## // `nix2vast` // options

Take a look at the full list of options available [here](./options.md).
